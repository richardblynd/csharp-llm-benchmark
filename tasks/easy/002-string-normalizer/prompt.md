Write a public class named `Solution` with this method:

```csharp
public static string Execute(string input)
```

Normalize `input` by trimming leading and trailing whitespace, replacing every run
of one or more whitespace characters with a single regular space, and converting
the result to lowercase using invariant culture.

Rules:
- Do not include a namespace.
- Do not use external libraries.
- If `input` is empty or whitespace only, return an empty string.
