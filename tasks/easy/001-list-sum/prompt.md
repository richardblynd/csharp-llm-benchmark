Write a public class named `Solution` with this method:

```csharp
public static int Execute(IReadOnlyList<int> numbers)
```

Return the sum of all integers in `numbers`.

Rules:
- Treat an empty list as a sum of `0`.
